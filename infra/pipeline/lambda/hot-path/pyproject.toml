[project]
name = "relic-hot-path"
version = "0.1.0"
description = "Kinesis -> DynamoDB hot path for relic"
requires-python = "==3.12.*"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "boto3>=1.35",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
